import React, { Component } from 'react';

export default class PhotoPreview extends Component {
    render() {
        return (
            <div>
            preview
            </div>
        );
    }
}
